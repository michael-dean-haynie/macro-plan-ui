import { MeasurementsPipe } from './measurements.pipe';

describe('MeasurementsPipe', () => {
  it('create an instance', () => {
    const pipe = new MeasurementsPipe();
    expect(pipe).toBeTruthy();
  });
});
